<div class="d-flex flex-row">
  <%= render "shared/sessions/sidebar" %>
  <div class="container">
    <p class='flash'><%= flash[:notice] %></p>
    <h2>Delete Account Page</h2>
    <p>Are user suer ? </p>
    <%= button_to 'Delete Account', delete_path, method: 'delete', data: {confirm: "Are you sure you want to delete this account?" } %>
  </div>
</div>
